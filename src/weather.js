class Weather {
  constructor(icon, date, temp) {
    this.icon = icon;
    this.date = date;
    this.temp = temp;
    // more to be added once the object has been more examined
  }

  buildCard(obj) {
    /* p's and divs created, assigned classes, innerText equal to properties from the weather object */
  }
}
